Today I learned about how to create a web app using python coding.

To do so we use the function flask and call it using a variable the value Flask(__name__) and show something on the page, you call it using @<variablename>.route().

You can also call other files and use templates. To do so, we imported render_template from flask and created a folder called template. Then, to call the file we did render_template('hello.html').